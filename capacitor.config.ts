import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'win-the-day-actual',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
